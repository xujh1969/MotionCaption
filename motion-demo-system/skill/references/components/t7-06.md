# t7-06 - 半环形占比仪表盘

- Component version: 1
- Use for: Single completion rate, score, or percentage from 0-100 percent.
- Avoid: Avoid large datasets, precision analysis, complex axes, and multiple metrics.
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `labelText`: text; required: yes; default: `"关键占比"`; semantic role: label; constraints: none beyond the declared type.
- `percent`: number; required: yes; default: `85`; semantic role: metric; constraints: minimum 0; maximum 100.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t7-06",
  "componentVersion": 1,
  "content": {
    "labelText": "关键占比",
    "percent": 85
  },
  "placementPreset": "auto",
  "role": "metric"
}
```
