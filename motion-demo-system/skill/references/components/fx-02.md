# fx-02 - 英文标签+主标题+渐变线+线下方小字

- Component version: 1
- Use for: Chapter title with a short supporting line.
- Avoid: Avoid long body copy and data.
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `enText`: text; required: yes; default: `"OPEN SKILL RELEASE"`; semantic role: label; constraints: none beyond the declared type.
- `subText`: text; required: yes; default: `"SKILL：现在公開"`; semantic role: label; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"现在{{公开}}"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "fx-02",
  "componentVersion": 1,
  "content": {
    "enText": "OPEN SKILL RELEASE",
    "subText": "SKILL：现在公開",
    "titleText": "现在{{公开}}"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
