# t3-02 - 前缀标签-数值-单位单行

- Component version: 1
- Use for: Single metric with a prefix label and unit.
- Avoid: Avoid unrelated prose, processes, lists, timelines, charts, and content beyond the stated capacity.
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `descText`: text; required: yes; default: `"核心指标对比基准"`; semantic role: body; constraints: none beyond the declared type.
- `tagText`: text; required: yes; default: `"算力提升"`; semantic role: label; constraints: none beyond the declared type.
- `unitText`: text; required: yes; default: `"%"`; semantic role: label; constraints: none beyond the declared type.
- `value`: number; required: yes; default: `58`; semantic role: metric; constraints: minimum 1; maximum 9999.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t3-02",
  "componentVersion": 1,
  "content": {
    "descText": "核心指标对比基准",
    "tagText": "算力提升",
    "unitText": "%",
    "value": 58
  },
  "placementPreset": "auto",
  "role": "label"
}
```
