# t1-07 - 顶部标签+主标题+辅助说明

- Component version: 1
- Use for: Section label, main title, and one-line explanation for a feature introduction.
- Avoid: Avoid metrics, processes, lists, charts, and long overflow text.
- Motion feel: 顶部小标先淡入，主标题与说明文字依次上浮；三行快节奏，适合功能条目。
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `descText`: text; required: yes; default: `"以数据为核心，重构智能运行逻辑"`; semantic role: body; constraints: none beyond the declared type.
- `kickerText`: text; required: yes; default: `"DATA DRIVEN"`; semantic role: label; constraints: none beyond the declared type.
- `topText`: text; required: yes; default: `"数据驱动智能升级"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t1-07",
  "componentVersion": 1,
  "content": {
    "descText": "以数据为核心，重构智能运行逻辑",
    "kickerText": "DATA DRIVEN",
    "topText": "数据驱动智能升级"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
