# t2-01 - 单侧渐变发光边框文本卡片

- Component version: 1
- Use for: Key viewpoint, quotation, reminder, or conclusion card; use 1-3 body lines.
- Avoid: Avoid lists, processes, metric comparisons, charts, and long paragraphs.
- Motion feel: —
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `contentText`: text; required: yes; default: `"核心技术突破，实现行业领先层级效能"`; semantic role: body; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t2-01",
  "componentVersion": 1,
  "content": {
    "contentText": "核心技术突破，实现行业领先层级效能"
  },
  "placementPreset": "auto",
  "role": "body"
}
```
