# t6-04 - 单输入双分支分叉流向

- Component version: 1
- Use for: Single input with two branch paths.
- Avoid: Avoid unrelated lists, timelines, charts, and long paragraphs.
- Motion feel: —
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `aText`: text; required: yes; default: `"高速路径"`; semantic role: label; constraints: none beyond the declared type.
- `bText`: text; required: yes; default: `"深度路径"`; semantic role: label; constraints: none beyond the declared type.
- `inText`: text; required: yes; default: `"输入任务"`; semantic role: label; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"分支调度逻辑"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t6-04",
  "componentVersion": 1,
  "content": {
    "aText": "高速路径",
    "bText": "深度路径",
    "inText": "输入任务",
    "titleText": "分支调度逻辑"
  },
  "placementPreset": "auto",
  "role": "title"
}
```
