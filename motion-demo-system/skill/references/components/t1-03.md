# t1-03 - 顶部小字注解+主标题

- Component version: 1
- Use for: Small annotation plus core conclusion with knowledge or fact emphasis.
- Avoid: Avoid metrics, processes, lists, charts, and long overflow text.
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `noteText`: text; required: yes; default: `"核心技术亮点"`; semantic role: body; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"全域智能感知系统"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t1-03",
  "componentVersion": 1,
  "content": {
    "noteText": "核心技术亮点",
    "titleText": "全域智能感知系统"
  },
  "placementPreset": "auto",
  "role": "body"
}
```
