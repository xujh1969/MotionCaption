# t2-03 - 弱底色高亮信息模块

- Component version: 1
- Use for: Title plus short explanation for a concept, feature, or key information.
- Avoid: Avoid lists, processes, metric comparisons, charts, and long paragraphs.
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `descText`: text; required: yes; default: `"低功耗、高效率、高兼容的一体化运行体系"`; semantic role: body; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"核心优势亮点"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t2-03",
  "componentVersion": 1,
  "content": {
    "descText": "低功耗、高效率、高兼容的一体化运行体系",
    "titleText": "核心优势亮点"
  },
  "placementPreset": "auto",
  "role": "title"
}
```
