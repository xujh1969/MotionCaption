# t7-10 - 迷你波形图(实时波动HUD)

- Component version: 1
- Use for: Real-time signal, data stream, or performance fluctuation shown as a HUD waveform.
- Avoid: Avoid categorical comparisons, large datasets, and precise labelled axes.
- Motion feel: 波形自左向右一次性绘制（约 650ms），绘制完成后持续循环微小上下波动，模拟实时信号。
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `tickLabels`: text; required: yes; default: `"0s|1s|2s|3s"`; semantic role: label; constraints: none beyond the declared type.
- `wavePoints`: text; required: yes; default: `"0.2,0.7,0.4,0.8,0.3,0.6,0.4,0.9,0.5,0.7"`; semantic role: label; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t7-10",
  "componentVersion": 1,
  "content": {
    "tickLabels": "0s|1s|2s|3s",
    "wavePoints": "0.2,0.7,0.4,0.8,0.3,0.6,0.4,0.9,0.5,0.7"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
