# fx-05 - 竖线+胶囊标签+双行标题

- Component version: 1
- Use for: Two-line theme title with category tags; use 1-3 tags.
- Avoid: Avoid long paragraphs and more than 3 tags.
- Capacity: 1-3 items

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `tags`: array of objects { t: string }; required: yes; default: `[{"t":"小标签一"},{"t":"小标签二"},{"t":"小标签三"}]`; semantic role: items; constraints: 1-3 items; all listed item fields are required strings.
- `tagText`: text; required: yes; default: `"标签标签"`; semantic role: label; constraints: none beyond the declared type.
- `title1`: text; required: yes; default: `"这是我的主标题"`; semantic role: title; constraints: none beyond the declared type.
- `title2`: text; required: yes; default: `"标题的第二行"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "fx-05",
  "componentVersion": 1,
  "content": {
    "tags": [
      {
        "t": "小标签一"
      },
      {
        "t": "小标签二"
      },
      {
        "t": "小标签三"
      }
    ],
    "tagText": "标签标签",
    "title1": "这是我的主标题",
    "title2": "标题的第二行"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
