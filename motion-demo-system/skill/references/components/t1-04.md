# t1-04 - 小标题-主标题-副标题层级

- Component version: 1
- Use for: Three-level section hierarchy.
- Avoid: Avoid metrics, processes, lists, charts, and long overflow text.
- Motion feel: 小标题、主标题、副标题三层逐层上浮入场，每层间隔约 0.5s；标准章节层级。
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `descText`: text; required: yes; default: `"突破传统算力瓶颈，实现全域效率提升"`; semantic role: title; constraints: none beyond the declared type.
- `tagText`: text; required: yes; default: `"TECH PROGRESS"`; semantic role: title; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"算力效能全面升级"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t1-04",
  "componentVersion": 1,
  "content": {
    "descText": "突破传统算力瓶颈，实现全域效率提升",
    "tagText": "TECH PROGRESS",
    "titleText": "算力效能全面升级"
  },
  "placementPreset": "auto",
  "role": "title"
}
```
