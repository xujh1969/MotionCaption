# t1-01 - 顶部标签+英文标签+主标题副标题

- Component version: 1
- Use for: Chapter opening or technology topic introduction.
- Avoid: Avoid metrics, processes, lists, charts, and long overflow text.
- Motion feel: 右侧四层文字（标签/英文/主标题/副标题）逐层上浮入场，每层间隔约 0.5s，顶部标签带呼吸。
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `enText`: text; required: yes; default: `"TECHNOLOGY ANALYSIS"`; semantic role: label; constraints: none beyond the declared type.
- `subText`: text; required: yes; default: `"基于深度学习的全新算力优化方案"`; semantic role: title; constraints: none beyond the declared type.
- `tagText`: text; required: yes; default: `"LOOP"`; semantic role: label; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"AI智能算法迭代升级"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t1-01",
  "componentVersion": 1,
  "content": {
    "enText": "TECHNOLOGY ANALYSIS",
    "subText": "基于深度学习的全新算力优化方案",
    "tagText": "LOOP",
    "titleText": "AI智能算法迭代升级"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
