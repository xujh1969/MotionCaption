# fx-08 - 大标题+多进度条

- Component version: 1
- Use for: Progress, score, or percentage comparison with 2-4 bars.
- Avoid: Avoid non-comparative prose and more than 4 bars.
- Motion feel: 外框淡入后标题单独淡入，进度条逐行从左向右生长并淡入（每行间隔约 0.4s）；运动感最强的 fx 组件。
- Capacity: 2-4 items

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `bars`: array of objects { g: 进度值 (string), n: 条目名称 (string) }; required: yes; default: `[{"g":"80","n":"小标题一"},{"g":"50","n":"小标题二"},{"g":"73","n":"小标题三"},{"g":"60","n":"小标题四"}]`; semantic role: items; constraints: 2-4 items; all listed item fields are required strings.
- `tagText`: text; required: yes; default: `"顶部标签"`; semantic role: label; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"大标题在此可编辑"`; semantic role: title; constraints: none beyond the declared type.

## Per-item timing (`at`)

Each item in `bars` carries a numeric `at` field — the item's appearance time in **seconds, relative to the scene's earliest cited cue start**. Items appear at their `at` time instead of the default even rhythm.

- **Every item that corresponds to one cited cue MUST get an `at`.** Compute it from the cue start times: `at = (cueStartMs − earliestCitedCueStartMs) / 1000`, rounded to 1 decimal. Include every cue you used in the scene's `sourceCueIds`.
- Worked example: a scene cites cues starting at 9800ms / 15600ms / 21400ms (earliest = 9800) and its bars rows visualize them → `at` values are `0`, `5.8`, `11.6`. The first item is `0`, and each later item appears exactly when its cue begins to be spoken.
- Omit `at` ONLY for an item that is a general summary not tied to any single cue (rare). Do NOT leave `at` out of items just to be safe, and do NOT invent evenly spaced values that no cue supports.
- `at` must be a number ≥ 0 (never a string, never negative, never beyond the scene duration). The example JSON below shows the field shape — replace the demo values with cue-derived ones.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "fx-08",
  "componentVersion": 1,
  "content": {
    "bars": [
      {
        "at": 0,
        "g": "80",
        "n": "小标题一"
      },
      {
        "at": 3.5,
        "g": "50",
        "n": "小标题二"
      },
      {
        "at": 7.2,
        "g": "73",
        "n": "小标题三"
      },
      {
        "at": 9.8,
        "g": "60",
        "n": "小标题四"
      }
    ],
    "tagText": "顶部标签",
    "titleText": "大标题在此可编辑"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
