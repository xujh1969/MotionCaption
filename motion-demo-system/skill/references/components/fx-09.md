# fx-09 - 深色渐变数字卡片

- Component version: 1
- Use for: Single core KPI, count, or growth value.
- Avoid: Avoid multiple metrics and paragraphs.
- Motion feel: 整卡淡入，核心数字滚动计数约 1.3s 并带渐变与呼吸微光，底部出处随后淡入；单 KPI 高光。
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `numText`: text; required: yes; default: `"48837"`; semantic role: metric; constraints: none beyond the declared type.
- `plusText`: text; required: yes; default: `"+"`; semantic role: label; constraints: none beyond the declared type.
- `sourceText`: text; required: yes; default: `"2026年累计AI 视频的播放量"`; semantic role: body; constraints: none beyond the declared type.
- `subText`: text; required: yes; default: `"全网观看数"`; semantic role: title; constraints: none beyond the declared type.
- `tagText`: text; required: yes; default: `"LAST EPISODE"`; semantic role: label; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "fx-09",
  "componentVersion": 1,
  "content": {
    "numText": "48837",
    "plusText": "+",
    "sourceText": "2026年累计AI 视频的播放量",
    "subText": "全网观看数",
    "tagText": "LAST EPISODE"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
