# t7-03 - 多段环形占比图

- Component version: 1
- Use for: Composition or proportion with 2-3 segments; total should be 100 percent.
- Avoid: Avoid large datasets, precision analysis, complex axes, and more than 3 segments.
- Motion feel: —
- Capacity: 2-3 items

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `centerText`: text; required: yes; default: `"总计"`; semantic role: label; constraints: none beyond the declared type.
- `segs`: array of objects { name: 名称 (string), pct: 占比(%) (string) }; required: yes; default: `[{"name":"推理算力","pct":"60"},{"name":"缓存开销","pct":"25"},{"name":"其他开销","pct":"15"}]`; semantic role: items; constraints: 2-3 items; all listed item fields are required strings.
- `titleText`: text; required: yes; default: `"算力资源分配"`; semantic role: title; constraints: none beyond the declared type.

## Per-item timing (`at`)

Each item in `segs` carries a numeric `at` field — the item's appearance time in **seconds, relative to the scene's earliest cited cue start**. Items appear at their `at` time instead of the default even rhythm.

- **Every item that corresponds to one cited cue MUST get an `at`.** Compute it from the cue start times: `at = (cueStartMs − earliestCitedCueStartMs) / 1000`, rounded to 1 decimal. Include every cue you used in the scene's `sourceCueIds`.
- Worked example: a scene cites cues starting at 9800ms / 15600ms / 21400ms (earliest = 9800) and its segs rows visualize them → `at` values are `0`, `5.8`, `11.6`. The first item is `0`, and each later item appears exactly when its cue begins to be spoken.
- Omit `at` ONLY for an item that is a general summary not tied to any single cue (rare). Do NOT leave `at` out of items just to be safe, and do NOT invent evenly spaced values that no cue supports.
- `at` must be a number ≥ 0 (never a string, never negative, never beyond the scene duration). The example JSON below shows the field shape — replace the demo values with cue-derived ones.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t7-03",
  "componentVersion": 1,
  "content": {
    "centerText": "总计",
    "segs": [
      {
        "at": 0,
        "name": "推理算力",
        "pct": "60"
      },
      {
        "at": 3.5,
        "name": "缓存开销",
        "pct": "25"
      },
      {
        "at": 7.2,
        "name": "其他开销",
        "pct": "15"
      }
    ],
    "titleText": "算力资源分配"
  },
  "placementPreset": "auto",
  "role": "title"
}
```
