# t5-02 - 三色状态标签条目清单

- Component version: 1
- Use for: Status-tagged task list with 2-3 items.
- Avoid: Avoid charts, long paragraphs, and more than 3 items.
- Capacity: 2-3 items

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `items`: array of objects { d: string, t: string, tag: string }; required: yes; default: `[{"d":"完成底层调度重写","t":"内核模块重构","tag":"DONE"},{"d":"兼容第三方模型接入","t":"多模型调度","tag":"PROGRESS"},{"d":"极端场景存在泄漏风险","t":"内存回收","tag":"WARN"}]`; semantic role: items; constraints: 2-3 items; all listed item fields are required strings.
- `titleText`: text; required: yes; default: `"迭代任务状态"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t5-02",
  "componentVersion": 1,
  "content": {
    "items": [
      {
        "d": "完成底层调度重写",
        "t": "内核模块重构",
        "tag": "DONE"
      },
      {
        "d": "兼容第三方模型接入",
        "t": "多模型调度",
        "tag": "PROGRESS"
      },
      {
        "d": "极端场景存在泄漏风险",
        "t": "内存回收",
        "tag": "WARN"
      }
    ],
    "titleText": "迭代任务状态"
  },
  "placementPreset": "auto",
  "role": "title"
}
```
