# t1-02 - 左侧竖线引用注释

- Component version: 1
- Use for: Quote, highlighted viewpoint, or key sentence; use 1-3 text lines.
- Avoid: Avoid metrics, processes, lists, charts, and long overflow text.
- Motion feel: 左侧竖线先自上而下生长，引用文字与底部注释先后淡入（注释轻微呼吸）；观点/引用质感。
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `noteText`: text; required: yes; default: `"行业核心共识 · 2026技术发展准则"`; semantic role: body; constraints: MUST be ≤ 34 CJK characters (fits 2 wrapped lines) — the note renders at ~25px wrapped inside 490px — over 34 CJK characters it wraps past 2 lines; MUST NOT contain sentence punctuation (，。、；：？！,.;:!?) — split parallel points into separate scenes or move the full sentence to a desc/note field.
- `quoteText`: text; required: yes; default: `"效率与体验的双向突破"`; semantic role: body; constraints: MUST be ≤ 20 CJK characters (fits 2 wrapped lines) — the quote renders at ~46px wrapped inside 490px — over 20 CJK characters it wraps past 2 lines and crowds the note below; MUST NOT contain sentence punctuation (，。、；：？！,.;:!?) — split parallel points into separate scenes or move the full sentence to a desc/note field.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t1-02",
  "componentVersion": 1,
  "content": {
    "noteText": "行业核心共识 · 2026技术发展准则",
    "quoteText": "效率与体验的双向突破"
  },
  "placementPreset": "auto",
  "role": "body"
}
```
