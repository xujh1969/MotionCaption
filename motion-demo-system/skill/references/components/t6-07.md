# t6-07 - 有序序号步骤列表·焦点滚动切换

- Component version: 1
- Use for: Vertical ordered steps where each step is narrated in its own cue and should be highlighted one by one (focus-scroll stepper).
- Avoid: Avoid scalar summaries, unordered lists, metric charts, and more than 5 items.
- Motion feel: 左侧安全区步骤列表（序号方块+连接线+步骤文本）：首条即高亮，之后每条目在自身时段渐入放大成当前焦点（前一条目同步缩小变白），连接线红色段逐段下行，末段整体 600ms 淡出。
- Capacity: 2-5 items

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `items`: array of objects { label: 步骤文本 (string) }; required: yes; default: `[{"label":"机器人"},{"label":"智能体"},{"label":"复杂软件"}]`; semantic role: items; constraints: 2-5 items; all listed item fields are required strings.
- `titleText`: text; required: yes; default: `"构建智能应用的三个阶段"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t6-07",
  "componentVersion": 1,
  "content": {
    "items": [
      {
        "label": "机器人"
      },
      {
        "label": "智能体"
      },
      {
        "label": "复杂软件"
      }
    ],
    "titleText": "构建智能应用的三个阶段"
  },
  "placementPreset": "auto",
  "role": "title"
}
```
