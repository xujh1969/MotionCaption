# t1-08 - 顶部锚点标签+主标题+正文段落

- Component version: 1
- Use for: Topic label, main title, and explanatory paragraph; use 2-4 body lines.
- Avoid: Avoid metrics, processes, lists, charts, and long overflow text.
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `bodyText`: text; required: yes; default: `"依托自适应算法，系统可实时调整运行策略，规避无效算力消耗，实现高效运转。"`; semantic role: body; constraints: none beyond the declared type.
- `tagText`: text; required: yes; default: `"CORE LOGIC"`; semantic role: label; constraints: none beyond the declared type.
- `titleText`: text; required: yes; default: `"核心运行逻辑解析"`; semantic role: title; constraints: none beyond the declared type.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t1-08",
  "componentVersion": 1,
  "content": {
    "bodyText": "依托自适应算法，系统可实时调整运行策略，规避无效算力消耗，实现高效运转。",
    "tagText": "CORE LOGIC",
    "titleText": "核心运行逻辑解析"
  },
  "placementPreset": "auto",
  "role": "label"
}
```
