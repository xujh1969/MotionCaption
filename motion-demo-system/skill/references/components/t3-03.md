# t3-03 - 双栏对比数据

- Component version: 1
- Use for: Two-value metric comparison.
- Avoid: Avoid unrelated prose, processes, lists, timelines, charts, and content beyond the stated capacity.
- Motion feel: —
- Capacity: scalar

## Editable content

Only the following keys may appear in `content`. All style and component layout properties are locked and must not be supplied by an Agent.

- `descL`: text; required: yes; default: `"基础算力水准"`; semantic role: body; constraints: none beyond the declared type.
- `descR`: text; required: yes; default: `"全新算力水准"`; semantic role: body; constraints: none beyond the declared type.
- `labelL`: text; required: yes; default: `"优化前"`; semantic role: label; constraints: none beyond the declared type.
- `labelR`: text; required: yes; default: `"优化后"`; semantic role: label; constraints: none beyond the declared type.
- `valueL`: number; required: yes; default: `45`; semantic role: metric; constraints: minimum 1; maximum 200.
- `valueR`: number; required: yes; default: `92`; semantic role: metric; constraints: minimum 1; maximum 200.

## Component JSON

This is legal JSON for the component object. Replace content values only with claims grounded in the selected source cues.

```json
{
  "componentId": "t3-03",
  "componentVersion": 1,
  "content": {
    "descL": "基础算力水准",
    "descR": "全新算力水准",
    "labelL": "优化前",
    "labelR": "优化后",
    "valueL": 45,
    "valueR": 92
  },
  "placementPreset": "auto",
  "role": "label"
}
```
